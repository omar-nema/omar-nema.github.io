{
  "name": "portfolio",
  "version": "0.0.1",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "svelte-kit build",
    "package": "svelte-kit package",
    "preview": "svelte-kit preview",
    "deploy": "npm run build && npx gh-pages -d build",
    "deploy2": "npm run build && cp -r build /tmp/portfolio-build && git checkout gh-pages && rm -rf * && cp -r /tmp/portfolio-build/* . && git add -A && git commit -m 'Deploy' && git push origin gh-pages && git checkout master && rm -rf /tmp/portfolio-build",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "next",
    "@sveltejs/adapter-static": "^1.0.0-next.26",
    "@sveltejs/kit": "next",
    "gh-pages": "^3.2.3",
    "prettier": "^3.6.2",
    "prettier-plugin-svelte": "^3.4.0",
    "svelte": "^3.44.0"
  },
  "type": "module",
  "dependencies": {
    "all": "^0.0.0"
  }
}
